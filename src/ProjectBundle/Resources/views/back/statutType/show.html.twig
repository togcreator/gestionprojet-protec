
<div class="modal-header">
	<button type="button" class="close" data-dismiss="modal">&times;</button>
	<h5 class="modal-title">{{ 'statutType.confirm_title'|trans([], 'statutType') }}</h5>
</div>

<div class="modal-body">
	<h2>{{ 'statutType.confirm_delete'|trans([], 'statutType')}}</h2>
	{{ form_start(delete_form, {'attr': {'id': 'statutType_form'}}) }}
	{{ form_end(delete_form) }}
</div>

<div class="modal-footer">
	<button type="button" class="btn btn-primary btn-danger" data-dismiss="modal">{{ 'statutType.close_button'|trans([], 'statutType') }}</button>
	<button type="button" class="btn btn-primary" onclick="jQuery('#statutType_form').submit()">{{ 'statutType.confirm_button'|trans([], 'statutType') }}</button>
</div>