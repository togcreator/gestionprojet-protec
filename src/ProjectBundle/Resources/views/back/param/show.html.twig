{% extends 'base.html.twig' %}

{% block body %}
    <h1>Param</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ param.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('back_param_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('back_param_edit', { 'id': param.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
