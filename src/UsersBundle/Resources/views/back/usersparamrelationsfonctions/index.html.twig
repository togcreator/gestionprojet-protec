{% extends 'base.html.twig' %}

{% block body %}
    {% include 'header.html.twig' %}
                <!-- /page header -->
                <div class="content">
                    <div class="row">

                         <!-- Page length options -->
                        <div class="panel panel-flat">
                            <div class="panel-heading">
                                <h5 class="panel-title">{{ 'usersparamrelationsfonctions.list'|trans({}, 'usersparamrelationsfonctions') }}</h5><br/>
                                <button type="button" class="btn bg-teal-400 btn-labeled legitRipple" onclick="document.location='{{ path('usersparamrelationsfonctions_new', {'idType': app.request.get('idType')})}}'">
                                    <b><i class="icon-add"></i></b> {{ 'usersparamrelationsfonctions.add_button'|trans({}, 'usersparamrelationsfonctions') }}
                                </button>

                                <div class="heading-elements">
                                    <ul class="icons-list">
                                        <li><a data-action="close" href="{{ path('usersparamrelationsfonctions_index')}}"></a></li>
                                    </ul>
                                </div>
                            </div>
                            <table class="table datatable-basic">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>{{ 'usersparamrelationsfonctions.label'|trans({}, 'usersparamrelationsfonctions') }}</th>
                                        <th>{{ 'global.actions'|trans }}</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {% if usersParams %}
                                    {% for param in usersParams %}
                                    <tr>
                                        <td><a href="{{ path('usersparamrelationsfonctions_show', { 'id': param.id }) }}">{{ param.id }}</a></td>
                                        <td>{{ param.label }}</td>
                                        <td class="text-center">
                                            <ul class="icons-list">
                                                <li class="dropdown">
                                                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                                        <i class="icon-menu9"></i>
                                                    </a>
                                                    <ul class="dropdown-menu dropdown-menu-right">
                                                        <li><a href="{{ path('usersparamrelationsfonctions_edit', { 'id': param.id }) }}"><i class="icon-pen"></i> {{ 'usersparamrelationsfonctions.edit_button'|trans({}, 'usersparamrelationsfonctions') }}</a></li>
                                                        <li><a href="{{ path('usersparamrelationsfonctions_delete', { 'id': param.id }) }}" data-target="#modal_remote" data-toggle="modal"><i class="icon-folder-remove"></i> {{ 'usersparamrelationsfonctions.del_button'|trans({}, 'usersparamrelationsfonctions') }}</a></li>
                                                    </ul>
                                                </li>
                                            </ul>
                                        </td>
                                    </tr>
                                    {% endfor %}
                                {% else %}
                                    <tr>
                                        <td>{{ 'usersparamrelationsfonctions.no_data'|trans({}, 'usersparamrelationsfonctions') }}</td>
                                    </tr>
                                {% endif %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
                
{% endblock %}
